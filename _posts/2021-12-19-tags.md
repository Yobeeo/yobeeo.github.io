---
layout: page
title: 标签
permalink: /tags/
---

文章标签分类较多，可在侧边栏目录中快速定位~

{% if page.settings.mermaid.enable %}

```mermaid
pie
{% for tag in site.tags %}
"{{ tag[0] }}" : {{ tag[1].size }}
{% endfor %}
```
{% endif %}

{% for tag in site.tags %}
## <span class="fa-layers fa-fw"><i class="fas fa-tag"></i> <span class="fa-layers-counter">{{ tag[1].size }}</span></span> {{ tag[0] }}

{% for post in tag[1] %}
- *{{ post.date | date_to_string }}* [{{ post.title }}]({{ post.url | relative_url }})
{% endfor %}
{% endfor %}
